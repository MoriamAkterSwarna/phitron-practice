#include<bits/stdc++.h>

using namespace std;

int main(){
    string s; 
    cin >> s; 
    cout << s[0] << endl; // prints the first character of the string 
    cout << s.at(0) << endl; // prints the first character of the string 
    cout << s.front() << endl; // prints the first character of the string

    cout << s[s.size() - 1] << endl; // prints the last character of the string 
    cout << s.back() << endl; // prints the last character of the string 

    
    




    return 0;
}